# سجل التغييرات - مشروع ERP Pro

## الإصدار 0.3.0 (2026-09-04)

> حملة الأمان والجودة الكاملة بعد التدقيق الشامل (14 SEC حرجة + 12 MED + بنية تحتية + جودة كود).

### أمان — P0 (ثغرات حرجة)
- SEC-01: إغلاق بوابات `/api/setup/*` العامة نهائياً (جلسة + دور مدير بعد الإعداد)
- SEC-03: منع سقوط النداءات الداخلية إلى صلاحيات Administrator (fail-closed + سجل تدقيق)
- SEC-04: قائمة سماح DocTypes (~150) + قائمة منع دائمة على البروكسي
- SEC-05: تدوير كل الأسرار + إزالتها من المستودع (`${VAR}` placeholders فقط)
- SEC-06: حسابات الديمو fail-closed (لا تُفعّل في الإنتاج أبداً)
- SEC-07: JWT secret إلزامي وقت التشغيل (حذف توليد البناء)
- SEC-08: نموذج cookie-first — لا توكن في localStorage
- SEC-09: تنقية HTML عبر DOMPurify لكل محتوى المستخدم
- SEC-10: إغلاق IDOR بوابة العميل (ربط المستخدم بعميله عبر جلسته)
- SEC-11: حماية من Open Redirect في تسجيل الدخول
- SEC-12: إزالة مسار التوكن القديم غير الموقّع
- SEC-13: إغلاق Path Traversal في حذف النسخ الاحتياطية
- SEC-14: إخفاء الأسرار في استجابات GET لإعدادات التكاملات

### أمان — P1 (ثغرات متوسطة)
- MED-01: حرس SSRF مشترك (حل DNS ومنع النطاقات الداخلية) على اختبار التكاملات وWebhooks + بوابات إدارية
- MED-02: مهلة 30s على نداءات الباك إند + إعادة محاولة على GET فقط (لا تكرار مستندات)
- MED-03: محدد معدل مشترك عبر Redis + استخراج IP بمنطق rightmost-trusted
- MED-04: التحقق من جلسة Frappe عند كل تجديد + jti وقائمة إبطال (logout/refresh يدوران التوكن)
- MED-05: سياسة الأمان المحفوظة تُطبّق فعلاً على الدخول (طول/تعقيد/محاولات/مدة جلسة)
- MED-06: تصدير التقارير خلف بوابة الصلاحيات نفسها
- MED-07: ترميز doctype في مسارات getDoc
- MED-08: سجل التدقيق للمدراء فقط + فشل صاخب بدل الصمت
- MED-09: تقييد نطاقات next/image بدل السماح للجميع
- MED-10: Service Worker بلا كاش لمسارات API + كاش مرتبط بالإصدار
- MED-11: مخزن الإعدادات بمخطط zod وملف مستقل (كان سيمحو علامة الإعداد!)
- MED-12: مقارنة CSRF ثابتة الزمن

### بنية تحتية (P3)
- Node 22-alpine + npm ci + بلا .env داخل الأرتيفاكت
- CI كامل: prisma validate + tsc + lint + vitest + **next build** + npm audit
- Redis ×3 بكلمة سر إلزامية + MariaDB بلا منفذ منشور (docker-compose وbunnyshell.yaml)
- docker-compose: بلا كلمات سر افتراضية + healthchecks + حدود ذاكرة + حذف version المهمل
- bns-init.sh: فشل صريح عند فشل new-site/migrate/HRMS (لا نظام مكسور بصمت)
- قفل حزم موحد npm (حذف bun.lock) + packageManager معلن
- حذف الملفات الميتة: 12 سكربت بيئة قديمة + railway/ + nixpacks + examples/websocket + mini-services + skills/ (من التتبع) + ملفات دافترى الضخمة
- حذف نموذجي Prisma القالبية User/Post
- نسخ احتياطي ليلي تلقائي (bench backup --with-files، استبقاء 7)

### ميزات أصبحت حقيقية (كانت وهمية)
- F-01: تصدير Excel/PDF في مركز التقارير يعمل فعلاً
- F-02: استيراد أرصدة الافتتاح من Excel حقيقي (تحليل xlsx)
- F-03/F-04: اختبار اتصال بوابات الدفع والمتاجر — نتائج حقيقية لا "نجح" دائماً
- F-05: إرسال كشف حساب العميل بالبريد عبر SMTP المخزن
- F-06: قائمتا مستودعات الإعدادات مملوءتان من ERPNext
- F-07: استوديو سير العمل عرض-فقط صادق حتى الربط الكامل

### جودة كود
- QUA-01: مسارات الطباعة المكسورة أُصلحت
- QUA-03: مؤشرات لوحات التحكم بخادمياً مجمّعاً (لا "آخر 100 مستند")
- QUA-04/05: فصل خريطة إعدادات HR/POS + توحيد صيغة الشركة الافتراضية
- QUA-06: عنوان WebSocket من env (لا localhost) + callbacks مستقرة
- QUA-07: منسق تواريخ مركزي بالعربية (استبدال toLocaleDateString en-US)
- QUA-08: aria-labels لأزرار الأيقونات في الشاشات الحرجة
- QUA-09: إصلاح الروابط الميتة /accounting/bank-and-cash
- QUA-11: الضريبة السريعة والمستودعات في فاتورة الشراء من API
- QUA-12: ترقيم صفحات خادمي للفواتير الدورية والعمولات

### توثيق
- توحيد الوثائق: PROJECT_STATUS.md مصدر الحقيقة الوحيد + أرشفة 9 وثائق متضاربة في docs/archive/
- CHANGELOG محدث (كان جامداً منذ 2026-04-30)

---


## الإصدار 0.2.0 (2026-04-30)

### الإنجازات الرئيسية

#### المرحلة 0: إصلاح الأساسيات (إنجاز جزئي ~60%)
- **حماية المسارات**: إنشاء `proxy.ts` كبوابة حماية لجميع المسارات المحمية، يتضمن:
  - التحقق من `erp_session` cookie في كل طلب
  - إعادة توجيه غير المصادقين إلى `/login` مع حفظ المسار الأصلي
  - فك تشفير رمز الجلسة والتحقق من انتهاء الصلاحية
  - استثناء المسارات العامة (login, api, static files)

- **نظام المصادقة**: إنشاء نظام مصادقة تجريبي شامل:
  - صفحة تسجيل دخول احترافية بتصميم RTL مع خط Cairo
  - أزرار دخول تجريبي سريع لكل دور
  - دعم 4 أدوار: مدير النظام (admin)، المحاسب (accountant)، مدير المبيعات (sales)، مدير الموارد البشرية (hr)
  - نسيان كلمة المرور (واجهة + API)
  - تخزين مزدوج للجلسة (localStorage + cookie)
  - API تسجيل دخول وخروج والتحقق من الهوية

- **الهيكل الأساسي**: إنشاء المكونات الأساسية:
  - `AppSidebar` - شريط جانبي مع تنقل بين الوحدات (10 وحدات)
  - `AppHeader` - شريط علوي مع بحث شامل ومستخدم
  - `DataTable` - جدول بيانات متقدم مع فرز وفلترة
  - `KpiCard` - بطاقات مؤشرات الأداء مع رسوم بيانية مصغرة
  - `DocForm` - نموذج مستند عام
  - `DynamicRowTable` - جدول صفوف ديناميكي (للفواتير والقيود)
  - `StatusBadge` - شارات الحالة بألوان مختلفة
  - `GlobalSearch` - بحث شامل عبر جميع الوحدات
  - `ExportButton` - تصدير البيانات (CSV, Excel, PDF)
  - `ErrorBoundary` / `PageSkeleton` - معالجة الأخطاء والتحميل

#### المرحلة 1: البنية التحتية (إنجاز جزئي ~30%)
- **API Routes**: إنشاء طبقة API شاملة:
  - `/api/auth/login` - تسجيل دخول تجريبي مع إعداد cookie
  - `/api/auth/logout` - تسجيل خروج مع مسح الجلسة
  - `/api/auth/me` - التحقق من الجلسة الحالية
  - `/api/auth/forgot-password` - نسيان كلمة المرور
  - `/api/data/[doctype]` - CRUD عام (GET: قائمة، POST: إنشاء)
  - `/api/data/[doctype]/[name]` - عمليات على مستند واحد (GET, PUT, DELETE)
  - `/api/data/[doctype]/bulk` - عمليات جماعية
  - `/api/dashboard/kpis` - مؤشرات لوحة التحكم
  - `/api/reports/[reportName]` - التقارير
  - `/api/method/[...path]` - وكيل لطرق ERPNext المخصصة

- **طبقة البيانات**: إنشاء أساس React Query:
  - `api.ts` - عميل API مع معالجة أخطاء شاملة
  - `hooks.ts` - React Query hooks (useDocList, useDoc)
  - `query-client.ts` - إعدادات Query Client مع إعادة المحاولة

- **اتصال ERPNext**: إنشاء `backend.ts` للاتصال بخادم ERPNext (إعداد أساسي)

- **الأنواع والمساعدات**:
  - `types.ts` - تعريفات الأنواع الأساسية للمستندات والمستخدمين
  - `helpers.ts` - دوال مساعدة (تنسيق التاريخ، العملة، الأرقام)
  - `utils.ts` - أدوات عامة (cn، formatCurrency)

#### الواجهات الأمامية (إنجاز ~35%)
- إنشاء **57 صفحة لوحة تحكم** تغطي جميع الوحدات:
  - المحاسبة: 8 صفحات (دليل الحسابات، القيود، الفواتير، المدفوعات، المصروفات، مراكز التكلفة، الأصول، الشيكات)
  - المبيعات: 7 صفحات (العملاء، الموردون، عروض الأسعار، أوامر البيع، مذكرات التسليم، نقاط البيع، أوامر الشراء)
  - المشتريات: 5 صفحات (أوامر الشراء، فواتير الشراء، عروض الموردين، طلبات الشراء، إيصالات الاستلام)
  - المخزون: 6 صفحات (الأصناف، المستودعات، حركات المخزون، الجرد، مستويات المخزون، قوائم الأسعار)
  - التصنيع: 4 صفحات (قوائم المواد BOM، أوامر العمل، خطط الإنتاج، محطات العمل)
  - الموارد البشرية: 11 صفحة (الموظفون، الحضور، الإجازات، أنواع الإجازات، العطلات، الورديات، العقود، قسائم الرواتب، هياكل الرواتب، الهيكل التنظيمي، طلبات الموظفين)
  - CRM: 5 صفحات (العملاء المحتملون، الفرص، الأنشطة، المتابعات، المواعيد)
  - العمليات: 3 صفحات (أوامر الشغل، تتبع الوقت، الإيجارات)
  - الإعدادات: 5 صفحات (رئيسية، طرق الدفع، معدلات الضرائب، قواعد الضرائب، الفروع)
  - أخرى: صفحة التقارير + سجل العمليات

- إنشاء **صفحة رئيسية** مع مؤشرات أداء وبطاقات إحصائية
- إنشاء **صفحة تسجيل دخول** احترافية مع خلفية متدرجة

#### التوثيق
- إنشاء 5 ملفات توثيق شاملة:
  - `docs/PROJECT_IDEA.md` - فكرة المشروع والرؤية والجمهور المستهدف
  - `docs/WORK_MECHANISM.md` - آلية عمل النظام والمعمارية وتدفق البيانات
  - `docs/PROJECT_STRUCTURE.md` - هيكل المشروع وإحصائيات مفصلة
  - `docs/CHANGELOG.md` - سجل التغييرات والإصدارات
  - `docs/PROJECT_STATUS.md` - حالة المشروع ونسبة الإنجاز
- إنشاء `DEVELOPMENT_PLAN.md` - خطة التطوير الشاملة (13 مرحلة)
- تحليل منافس (دفترة) مع مقارنة الفجوات

#### النشر والبنية التحتية
- `Dockerfile` - بناء حاوية Docker للتطبيق
- `docker-compose.yml` - تشغيل متكامل (Next.js + ERPNext + MariaDB + Redis + Caddy)
- `Caddyfile` - إعدادات الخادم الوكيل مع SSL تلقائي
- `prisma/schema.prisma` - مخطط قاعدة البيانات

### المشاكل المعروفة

1. ~~**أخطاء TypeScript**~~ — **مُحدَّث (M-21):** `ignoreBuildErrors: false`؛ راجع [`system_fixes_tracker.md`](./system_fixes_tracker.md). قد تبقى تحسينات نوعية عرضية.
2. **نظام المصادقة**: يستخدم Base64 بدلاً من JWT (غير آمن للإنتاج)
3. **بيانات تجريبية**: معظم الصفحات تعرض بيانات ثابتة (hardcoded) بدون اتصال حقيقي بـ ERPNext
4. **صفحات قائمة فقط**: أغلب الصفحات تعرض قوائم بدون نماذج إنشاء/تعديل كاملة
5. **مشكلة تسجيل الدخول**: في بيئة البروكسي (Caddy) قد لا تعمل ملفات تعريف الارتباط بشكل صحيح
6. **لا توجد اختبارات**: لا يوجد أي اختبارات وحدة أو تكامل
7. **تكرار في الكود**: دوال مكررة مثل formatCurrency و cn في ملفات مختلفة
8. **أيقونة خارجية**: رابط أيقونة من z-cdn.chatglm.cn يجب استبداله بأيقونة محلية

### التغييرات التقنية

- Next.js 16.1.1 مع App Router
- React 19 + TypeScript 5
- Tailwind CSS 4 + shadcn/ui
- Zustand 5 لإدارة الحالة
- React Query 5 لإدارة البيانات
- Recharts 2 للرسوم البيانية
- Prisma 6 ORM مع MariaDB
- Docker Compose للنشر المتكامل
- Caddy كخادم وكيل مع SSL تلقائي

---

## الإصدار 0.1.0 (2026-04-29)

### الإنجازات الأولية

- إنشاء مشروع Next.js أساسي مع App Router
- إعداد Tailwind CSS وshadcn/ui مع دعم RTL
- إنشاء الهيكل العام للصفحات (57 صفحة)
- إضافة دعم RTL واللغة العربية مع خط Cairo
- إنشاء تصميم الشريط الجانبي والعلوي
- إنشاء صفحات لوحة التحكم الأولية
- إعداد Docker Compose وDockerfile
- إنشاء خطة التطوير الشاملة (DEVELOPMENT_PLAN.md)
- تحليل منافس (دفترة) وتحديد الفجوات (daftra_features_analysis.md)
- إنشاء نظام مصادقة تجريبي أساسي
