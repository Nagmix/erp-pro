# ERP Pro - نظام إدارة موارد المؤسسات

نظام ERP متكامل مبني بتقنية Next.js مع واجهة عربية عصرية.

## المعمارية

```
┌─────────────┐     ┌──────────────┐     ┌──────────────────┐
│  المستخدم   │────→│  Next.js     │────→│  الباك إند       │
│  (المتصفح)  │     │  (Frontend)  │     │  (داخلي - مخفي)  │
│             │←────│  Port: 3000  │←────│  Port: 8000      │
└─────────────┘     │              │     │  ┌────────────┐  │
                    │  API Routes  │     │  │ MariaDB    │  │
                    │  /api/*      │     │  │ Redis      │  │
                    │              │     │  │ Scheduler  │  │
                    └──────────────┘     │  │ Workers    │  │
                                         │  └────────────┘  │
                                         └──────────────────┘
```

## التشغيل

### الطريقة 1: Docker Compose (الإنتاج)

```bash
# إعداد أول مرة
./erp-pro.sh setup

# تشغيل
./erp-pro.sh start

# إيقاف
./erp-pro.sh stop

# حالة الخدمات
./erp-pro.sh status
```

### الطريقة 2: التطوير المحلي

```bash
# تشغيل الواجهة فقط (مع بيانات تجريبية)
bun run dev

# أو عبر السكريبت
./erp-pro.sh dev
```

### ERPNext بدون Docker (مؤقت)

راجع **`docs/ERPNEXT_LOCAL_WITHOUT_DOCKER.md`** (WSL2 + bench، إخفاء Desk، ربط المفاتيح من **`/settings/erp-backend`** أو سكربت `scripts/frappe-bootstrap-keys.mjs`).

## الوحدات

| الوحدة | الصفحات |
|--------|---------|
| المحاسبة والمالية | دليل الحسابات، القيود اليومية، فواتير المبيعات، فواتير المشتريات، المدفوعات |
| المبيعات والمشتريات | العملاء، الموردين، عروض الأسعار، أوامر البيع، أوامر الشراء |
| المخزون | الأصناف، المستودعات، حركة المخزون |
| الموارد البشرية | الموظفين، الحضور والانصراف، طلبات الإجازة، كشوف الرواتب |
| التصنيع | قوائم المواد (BOM)، أوامر العمل |

## التقنيات

- Next.js 16 + TypeScript
- Tailwind CSS + shadcn/ui
- Recharts (رسوم بيانية)
- Zustand (إدارة الحالة)
- Docker Compose (تشغيل متكامل)

## متغيرات البيئة

انسخ `.env.example` إلى `.env` وعدّل القيم:

```bash
cp .env.example .env
```
